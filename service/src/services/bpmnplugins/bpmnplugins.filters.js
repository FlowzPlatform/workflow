/* eslint no-console: 1 */
// console.warn('You are using the default filter for the bpmnplugins service.'); // eslint-disable-line no-console

module.exports = function (data, connection, hook) { // eslint-disable-line no-unused-vars
  return data;
};
